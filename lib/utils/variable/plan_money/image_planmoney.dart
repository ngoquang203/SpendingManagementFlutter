
import 'package:flutter/material.dart';

class ImagePlanMoney{
  final String? image;
  final Color? color;

  ImagePlanMoney(this.image, this.color);

  get getImage => image;
  get getColor => color;
}